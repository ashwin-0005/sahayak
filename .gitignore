# Dependencies (matches backend/ and frontend/ at any depth)
node_modules/

# Build output
dist/

# Environment files (never commit secrets; examples are tracked explicitly)
.env
.env.*
!.env.example

# Backend SQLite database files
backend/data/

# Test artifacts
*.test.db
*.test.db-journal
*.db-wal
*.db-shm
coverage/

# TypeScript build info (regenerated on every build)
*.tsbuildinfo
